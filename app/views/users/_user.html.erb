<li>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    &nbsp;| &nbsp;<%= link_to "Delete", user, method: :delete, data: { confirm: "Are you sure you want to delete #{user.name} permanently?" } %>
  <% end %>
</li>